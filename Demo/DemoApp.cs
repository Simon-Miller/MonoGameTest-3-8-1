using Graphics;
using MonoGameTest_3_8_1;

namespace Demo
{
    public class DemoApp : Game1
    {
        public DemoApp() : base() 
        {
            
            uint gameWidth = 400;
            uint gameHeight = 320;

            //var xxx = new ScreenDisplay(this.graphics, gameWidth, gameHeight);
        }

        protected override void Initialize()
        {
            base.Initialize();
        }

        //override 
    }
}